---
export interface Props {
  title: string;
  subtitle: string;
  image?: string;
}
const { title, subtitle, image = "/images/gallery/gallery-hero.jpg" } = Astro.props;
---

<section class="page-hero" aria-label="Frequently asked questions">
  <img src={image} alt="" />
  <div class="veil"></div>
  <div class="wrap copy">
    <p class="eyebrow light">Support</p>
    <h1>{title}</h1>
    <p>{subtitle}</p>
  </div>
</section>
